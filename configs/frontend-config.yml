# ==============================================================
# MGSPlus — Frontend Configuration (Vue 3 + Vite)
# Dev server port, API routing.
# ==============================================================

# ── Dev server ─────────────────────────────────────────────────
service:
  host: frontend
  port: 5173

# ── API routing ────────────────────────────────────────────────
api:
  base_url: /api
  backend_url: http://localhost:5001
  ws_url: ws://localhost:5001
